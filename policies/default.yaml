# openlock default sandbox policy.
#
# T1 threat model: assume the agent is malicious.
# Default-deny egress. Only the endpoints below are reachable.
# Per-binary credential scoping prevents cross-provider leakage.

version: 1
filesystem_policy:
  include_workdir: true
  read_only:
    - /usr
    - /lib
    - /proc
    - /dev/urandom
    - /app
    - /etc
    - /var/log
  read_write:
    - /sandbox
    - /tmp
    - /dev/null
landlock:
  compatibility: best_effort
process:
  run_as_user: sandbox
  run_as_group: sandbox
network_policies:
  claude_code:
    binaries:
      - path: /usr/local/bin/claude
      - path: /usr/local/bin/node
    endpoints:
      - host: api.anthropic.com
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: POST
              path: /v1/**
          - allow:
              method: GET
              path: /api/hello
        cred_inject:
          provider: anthropic
          strip_headers:
            - Authorization
            - x-api-key
            - Cookie
          inject:
            - header: Authorization
              from_credential: ANTHROPIC_BEARER_TOKEN
              value_prefix: 'Bearer '
      - host: platform.claude.com
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: GET
              path: /v1/oauth/hello
        cred_inject:
          provider: anthropic
          strip_headers:
            - Authorization
            - x-api-key
            - Cookie
          inject:
            - header: Authorization
              from_credential: ANTHROPIC_BEARER_TOKEN
              value_prefix: 'Bearer '
    allowed_secrets:
      - ANTHROPIC_BEARER_TOKEN
  opencode:
    binaries:
      - path: /usr/local/bin/opencode
      - path: /usr/local/bin/node
    endpoints:
      - host: openrouter.ai
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: POST
              path: /api/v1/**
        cred_inject:
          provider: openrouter
          strip_headers:
            - Authorization
            - x-api-key
            - Cookie
          inject:
            - header: Authorization
              from_credential: OPENROUTER_BEARER_TOKEN
      - host: models.dev
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: GET
              path: /**
      - host: models.opencode.ai
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: GET
              path: /**
    allowed_secrets:
      - OPENROUTER_BEARER_TOKEN
  pi:
    binaries:
      - path: /usr/local/bin/pi
      - path: /usr/local/bin/node
    endpoints:
      - host: openrouter.ai
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: POST
              path: /api/v1/**
        cred_inject:
          provider: openrouter
          strip_headers:
            - Authorization
            - x-api-key
            - Cookie
          inject:
            - header: Authorization
              from_credential: OPENROUTER_BEARER_TOKEN
    allowed_secrets:
      - OPENROUTER_BEARER_TOKEN
  copilot_cli:
    binaries:
      - path: /usr/local/lib/node_modules/@github/copilot/node_modules/@github/copilot-linux-arm64/copilot
      - path: /usr/local/lib/node_modules/@github/copilot/node_modules/@github/copilot-linux-x64/copilot
      - path: /usr/local/bin/node
    endpoints:
      - host: api.github.com
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: GET
              path: /copilot_internal/user
          - allow:
              method: GET
              path: /copilot_internal/managed_settings
        cred_inject:
          provider: github_copilot
          strip_headers:
            - Authorization
            - x-api-key
            - Cookie
          inject:
            - header: Authorization
              from_credential: GITHUB_COPILOT_PAT
              value_prefix: 'Bearer '
      - host: api.individual.githubcopilot.com
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: GET
              path: /models
          - allow:
              method: POST
              path: /models/session
          - allow:
              method: POST
              path: /chat/completions
          - allow:
              method: POST
              path: /responses
          - allow:
              method: POST
              path: /models/session/intent
          - allow:
              method: POST
              path: /v1/messages
          - allow:
              method: POST
              path: /mcp/readonly
          - allow:
              method: POST
              path: /auto
        cred_inject:
          provider: github_copilot
          strip_headers:
            - Authorization
            - x-api-key
            - Cookie
          inject:
            - header: Authorization
              from_credential: GITHUB_COPILOT_PAT
              value_prefix: 'Bearer '
      - host: api.business.githubcopilot.com
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: GET
              path: /models
          - allow:
              method: POST
              path: /models/session
          - allow:
              method: POST
              path: /chat/completions
          - allow:
              method: POST
              path: /responses
          - allow:
              method: POST
              path: /models/session/intent
          - allow:
              method: POST
              path: /v1/messages
          - allow:
              method: POST
              path: /mcp/readonly
          - allow:
              method: POST
              path: /auto
        cred_inject:
          provider: github_copilot
          strip_headers:
            - Authorization
            - x-api-key
            - Cookie
          inject:
            - header: Authorization
              from_credential: GITHUB_COPILOT_PAT
              value_prefix: 'Bearer '
      - host: api.enterprise.githubcopilot.com
        port: 443
        protocol: rest
        enforcement: enforce
        rules:
          - allow:
              method: GET
              path: /models
          - allow:
              method: POST
              path: /models/session
          - allow:
              method: POST
              path: /chat/completions
          - allow:
              method: POST
              path: /responses
          - allow:
              method: POST
              path: /models/session/intent
          - allow:
              method: POST
              path: /v1/messages
          - allow:
              method: POST
              path: /mcp/readonly
          - allow:
              method: POST
              path: /auto
        cred_inject:
          provider: github_copilot
          strip_headers:
            - Authorization
            - x-api-key
            - Cookie
          inject:
            - header: Authorization
              from_credential: GITHUB_COPILOT_PAT
              value_prefix: 'Bearer '
    allowed_secrets:
      - GITHUB_COPILOT_PAT
  npm_packages:
    binaries:
      - path: /usr/local/bin/npm
      - path: /usr/local/bin/node
      - path: /usr/local/bin/npx
    endpoints:
      - host: registry.npmjs.org
        port: 443
        protocol: rest
        enforcement: enforce
        access: read-only
        allow_encoded_slash: true
        trust_check:
          registry: npm
    allowed_secrets: []
  python_packages:
    binaries:
      - path: /usr/local/bin/uv
      - path: /usr/local/bin/uvx
      - path: /usr/bin/python3
    endpoints:
      - host: pypi.org
        port: 443
        protocol: rest
        enforcement: enforce
        access: read-only
        trust_check:
          registry: pypi
      - host: files.pythonhosted.org
        port: 443
        protocol: rest
        enforcement: enforce
        access: read-only
        trust_check:
          registry: pypi
    allowed_secrets: []
